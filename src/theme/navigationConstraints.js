import { StyleSheet } from 'react-native';

const navigationConstraints = StyleSheet.create({
  headerLeft: {
    marginLeft: 17,
  },
  headerRight: {
    marginRight: 16,
  }
});

export default navigationConstraints;
